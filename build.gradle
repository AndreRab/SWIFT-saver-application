plugins {
	id 'java'
	id 'org.springframework.boot' version '3.2.3'
	id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.Remitly.swift'
version = '0.0.1-SNAPSHOT'

bootJar {
	archiveFileName = 'app.jar'
}

task runDockerCompose(type: Exec, 'dependsOn': bootJar) {
	workingDir '.'
	commandLine 'docker-compose', 'up', '--build', '-d'
}

task compileAndTest(type: GradleBuild, dependsOn: runDockerCompose) {
	tasks = ['clean', 'build']
}

task deploy(dependsOn: compileAndTest) {
	doLast {
		println "Deploying..."
	}
}

task stopDockerCompose(type: Exec) {
	workingDir '.'
	commandLine 'docker-compose', 'stop'
}

task stop(dependsOn: stopDockerCompose) {
	doLast {
		println "Stopping..."
	}
}

java {
	sourceCompatibility = '17'
}

jar {
	enabled = false
}

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter'
	implementation 'org.springframework.boot:spring-boot-starter-web'
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	implementation 'org.springframework.boot:spring-boot-starter-validation'
	implementation 'mysql:mysql-connector-java:8.0.27'

	implementation 'com.opencsv:opencsv:5.7.1'

	implementation 'org.projectlombok:lombok'
	annotationProcessor 'org.projectlombok:lombok'

	testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

tasks.named('test') {
	useJUnitPlatform()
}
